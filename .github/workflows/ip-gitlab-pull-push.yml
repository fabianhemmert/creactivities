name: Docker Image CI

on:
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
        docker login gitlab.interactive-pioneers.de:5000 -u github-actions -p_PdaN3qs4RMDGmALbxoV
        docker pull gitlab.interactive-pioneers.de:5000/ips/creactivities/cms:2022-01
        docker tag docker tag gitlab.interactive-pioneers.de:5000/ips/creactivities/cms:2022-01 ghcr.io/fabianhemmert/creactivities/cms:0.0.2
        docker login  ghcr.io -u ${{ github.actor }}  -p ${{ secrets.GITHUB_TOKEN }}
        docker push ghcr.io/fabianhemmert/creactivities/cms:0.0.2
        
